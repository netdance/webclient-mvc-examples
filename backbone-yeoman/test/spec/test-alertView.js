/* global define, describe, it */

define(function(require) {
    'use strict';

    describe('Alerts', function() {
        it('should attach correctly');
        it('should receive triggered events ');
        it('should display success alerts on success event');
        it('should display error alert on error event');
        it('should display success message on unspecified event message');
        it('should display a reasonable default message if none provided');
    });
});