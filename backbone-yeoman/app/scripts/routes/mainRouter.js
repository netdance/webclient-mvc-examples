/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var MainrouterRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return MainrouterRouter;
});